In the name of God

# BACK (NODE JS)

> BACK-END TypeScript Boilerplate (EXPRESS Framework)

```batch
# install dependencies
$ npm install

# serve
$ npm start

# build for production
$ npm run build
$ npm run start:prod
```
